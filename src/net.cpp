#include "net.hpp"

void run_streamer(DbnReader&, const Options&) {
    // TODO: implement TCP server + text protocol + rate limiting
}

void run_engine(OrderBook&, const Options&) {
    // TODO: implement TCP client + protocol + metrics
}
